<div align="center">
  <h1>Хомо Готово</h1>
  <p><strong>Интеллектуальная платформа для планирования и проверки перепланировок квартир</strong></p>
</div>

---

**"Хомо Готово"** — это комплексный проект, который объединяет интерактивный веб-редактор для планов квартир с мощными возможностями искусственного интеллекта. Платформа позволяет пользователям не только визуализировать свои идеи по перепланировке в 2D и 3D, но и автоматически проверять их на соответствие строительным нормам и правилам (СНиП), используя как облачные, так и локальные большие языковые модели (LLM).

## 🚀 Ключевые преимущества

- **Интерактивный 2D/3D Редактор Планов:** Загружайте эскизы или создавайте планы с нуля, используя интуитивно понятные инструменты для расстановки и редактирования стен (несущих, перегородок, под снос), дверей, окон и 3D-моделей мебели. Мгновенно переключайтесь в полноценный 3D-режим для объемной визуализации с возможностью масштабирования и вращения.
- **AI-проверка на соответствие СНиП:** Система использует ИИ для анализа вашего плана и выявления потенциальных нарушений законодательства (снос несущих стен, неправильное расположение "мокрых зон" и т.д.).
- **AI-ассистент для идей:** Получайте креативные предложения по улучшению планировки от ИИ, который поможет оптимизировать пространство.
- **Гибкая архитектура (локальный + облачный ИИ):** Проект использует как Google Gemini API для быстрых проверок в вебе, так и локальный сервер Ollama для более глубокого, кастомизированного анализа, что обеспечивает гибкость и отказоустойчивость.
- **Масштабируемая система валидации:** Специализированный Python-модуль (`snip`) запускает 10 ИИ-агентов параллельно для глубокого и всестороннего анализа текстовых проектов перепланировки.
- **Продвинутая визуализация интерьеров:** Модуль `visualizer` позволяет сгенерировать единое изображение комнаты на основе выбранных ассетов (обои, пол, мебель), открывая новые возможности для экспериментов с дизайном и быстрого предпросмотра идей.
- **Модульная структура:** Проект четко разделен на независимые компоненты, что упрощает разработку и поддержку.

## 🏗️ Архитектура проекта

Проект состоит из трех основных независимых модулей:

1.  **`ui/` (Фронтенд-приложение)**
    *   **Технологии:** React, TypeScript, Vite, `@react-three/fiber`, `@react-three/drei`.
    *   **Описание:** Основной интерактивный интерфейс пользователя. Представляет собой SPA (Single Page Application) с мощным 2D-редактором на Canvas для создания детальных планов этажей (включая различные типы стен, двери, окна и мебель) и мгновенным переключением в 3D-режим для реалистичной визуализации. Поддерживает масштабирование, вращение и интерактивное размещение 3D-моделей. Включает каталог материалов и форм взаимодействия. Взаимодействует с Google Gemini API и локальным сервисом `llm` для проверки и генерации идей.

2.  **`llm/` (Сервис для Ollama)**
    *   **Технологии:** TypeScript.
    *   **Описание:** Изолированный сервис-обертка для взаимодействия с локально запущенным сервером Ollama. Предоставляет функции для генерации текста и проверки доступности моделей. Используется фронтендом.

3.  **`snip/` (Система пакетной валидации)**
    *   **Технологии:** Python, LangChain.
    *   **Описание:** Автономный модуль для глубокого анализа текстовых документов (`.md`). Запускает 10 ИИ-агентов, которые, используя `LangChain` и `Ollama`, проверяют документ на соответствие правилам из локальной базы знаний (`SNiP.md`).

4.  **`visualizer/` (Модуль визуализации)**
    *   **Технологии:** Python.
    *   **Описание:** Независимый ИИ-агент, который объединяет различные ассеты (обои, напольные покрытия, мебель) в единую "картину" интерьера комнаты, генерируя детализированное текстовое описание результата. Открывает возможности для исследования дизайна и композиции.

5.  **`bti_scan/` (Система консультации по нормативным документам)**
    *   **Технологии:** Python, FastAPI, PostgreSQL + pgvector, Docker, Nginx, GigaChat.
    *   **Описание:** Полноценное веб-приложение, реализующее RAG-систему для ответов на вопросы по нормативным документам. Оно индексирует PDF-файлы в векторной базе данных и использует GigaChat для генерации ответов на основе найденной информации. Полностью контейнеризировано с помощью Docker.

## 🛠️ Пошаговый запуск проекта

Для полного развертывания проекта выполните следующие шаги.

### Шаг 1: Настройка локальной LLM (Ollama)

Это основа для работы всех ИИ-функций проекта.

1.  **Установите Ollama,** если он у вас не установлен. Инструкции на официальном сайте: [https://ollama.com/](https://ollama.com/)

2.  **Скачайте необходимую модель.** В отдельном окне терминала выполните команду:
    ```bash
    ollama pull gemma3:27b
    ```

3.  **Запустите сервер Ollama с настройками CORS.** Это **критически важный** шаг, чтобы веб-интерфейс (`ui`) мог отправлять запросы к локальному серверу. В отдельном окне терминала выполните:
    ```bash
    OLLAMA_ORIGINS=http://localhost:3000 ollama serve
    ```
    *Держите этот терминал запущенным в фоновом режиме.*

### Шаг 2: Запуск веб-интерфейса (`ui`)

1.  **Перейдите в директорию `ui`:**
    ```bash
    cd ui
    ```

2.  **Установите зависимости Node.js:**
    ```bash
    npm install
    ```

3.  **(Опционально) Настройте Gemini API.** Для работы AI-ассистента через облако создайте файл `.env` в папке `ui` и добавьте в него ваш ключ:
    ```
    GEMINI_API_KEY=ВАШ_КЛЮЧ_ОТ_GOOGLE_AI_STUDIO
    ```
    *Если ключ не предоставить, приложение будет использовать локальные эвристики и Ollama.*

4.  **Запустите dev-сервер:**
    ```bash
    npm run dev
    ```
    После этого приложение будет доступно в браузере по адресу: **http://localhost:3000**

### Шаг 3: Запуск модуля валидации (`snip`)

Этот модуль запускается отдельно для анализа документов.

1.  **Перейдите в директорию `snip`** из корня проекта:
    ```bash
    cd snip
    ```

2.  **Установите зависимости Python:**
    (Рекомендуется использовать виртуальное окружение)
    ```bash
    pip install -r requirements.txt
    ```

3.  **Запустите скрипт анализа:**
        Убедитесь, что сервер Ollama из **Шага 1** все еще запущен.
        ```bash
        python main.py
        ```
        В консоли вы увидите отчеты о проверке документа `doc.md` от каждого из 10 агентов.

### Шаг 4: Запуск модуля визуализации (`visualizer`)

Этот модуль позволяет сгенерировать описание интерьера на основе предоставленных ассетов.

1.  **Подготовьте ассеты.** Поместите `.png` файлы (например, `wallpaper.png`, `floor.png`, `bed.png`) в директорию `visualizer/assets/` в корне проекта.
2.  **Запустите генерацию:** Из **корневой директории проекта** выполните:
    ```bash
    python visualizer/main.py
    ```
    В консоли вы увидите процесс "генерации" и сохранения результата.
3.  **Проверьте результат:** Сгенерированное описание интерьера будет сохранено в файле `visualizer/result.md`.

### Шаг 5: Запуск модуля `bti_scan`

Этот модуль представляет собой независимое RAG-приложение, упакованное в Docker.

1.  **Убедитесь, что у вас установлен Docker и Docker Compose.**

2.  **Перейдите в директорию `bti_scan`:**
    ```bash
    cd bti_scan
    ```

3.  **Создайте и настройте файлы окружения:**
    *   Создайте файл `.env` и добавьте в него токен для GigaChat:
        ```
        CHAT_TOKEN=ВАШ_ТОКЕН_ДЛЯ_GIGACHAT
        ```
    *   Создайте файл `db_password.txt` и запишите в него пароль для базы данных PostgreSQL:
        ```
        mysecretpassword
        ```

4.  **Запустите приложение с помощью Makefile:**
    Эта команда соберет образы и запустит все необходимые контейнеры (бэкенд, базу данных, nginx).
    ```bash
    make up-build
    ```
    После запуска API будет доступно через Nginx на порту `80`.
